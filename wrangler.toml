name = "supercompute"
compatibility_date = "2026-04-30"
pages_build_output_dir = "public"

[[d1_databases]]
binding = "DB"
database_name = "supercompute-db"
database_id = "e3c7c7f9-df4a-4e1b-9bc7-97f1faadf282"

[vars]
CORS_ORIGIN = "https://supercompute.io"

# KV for session/nonce storage — create with:
# wrangler弄 4.x: wrangler kv:namespace create CACHE
# For now, auth falls back to memory-only (sessions work, nonces expire per-request)